export default function taskBlock(trueOrFalse) {
  const task = false;
  const task2 = true;

// conditions
  if (trueOrFalse) {
    const task = true;
    const task2 = false;
  }

  // return statement
  return [task, task2];
}
